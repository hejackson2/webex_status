# webex_status_check.py

This will check the webex status of an email address every 30 seconds.  When the user has a state of 'active', meaning not on a call or in a meeting, it will display a notification pop-up and 'speak' the notification as well.
